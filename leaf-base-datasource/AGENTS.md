# AGENTS.md - leaf-base-datasource

本文件用于指导 `leaf-base-datasource` 模块的后续迭代。修改本模块时，必须优先保持现有功能、现有接口和生产环境兼容性，在此基础上再做完善。

## 通用硬性规则

1. 每次修改只能触碰当前 Maven 模块；确需跨模块时，先说明触发原因、受影响模块、替代方案和最小改动集合。
2. 现有代码已经在生产环境使用，不得随意更改对外接口规范、配置语义、默认行为、异常语义或序列化契约；必须变更时，同步补充兼容说明、迁移路径和回归测试。
3. 在确保原有功能正确的前提下完善实现；修改既有逻辑前，先用测试锁定当前成功、失败、空值、边界和回归路径。
4. 基础框架代码必须优先审查性能、线程安全、资源释放和并发可见性；线程池与虚拟线程都能满足需求时，优先使用虚拟线程。
5. 空值处理必须支持 JSpecify 规范：新增或修改 API、DTO、配置属性、回调、集合元素和异步结果时，明确 `@NullMarked`、`@Nullable` 或等价约束，让空值语义可被 IDE、编译检查和测试验证。
6. 严格遵循测试驱动最佳实践：先写或调整能暴露问题的测试，再实现最小代码变更，最后运行模块级验证命令。
7. 涉及改动的点都要检查测试用例是否完整；覆盖不足时补齐成功、失败、异常、空值、边界和回归场景。
8. 功能只有在 Spring Boot 应用运行后才能验证时，使用 `@SpringBootTest` 或等价方式模拟真实启动后的 Bean 装配、条件配置、AOP、监听器、异步流程和配置绑定。
9. 修改线程安全或性能相关代码时，必须补充严格测试；覆盖并发访问、重复执行、超时、取消、资源释放和关键指标，无法自动化时在提交说明写清手工压测命令、样本规模和观察指标。
10. 所有 `log` 日志内容必须使用 ASCII 字符，避免不同运行环境、终端编码或日志采集链路出现乱码。
11. 不要随便把已存在代码抽取成单独方法或类；只有抽取后的逻辑至少被两个位置复用，或能显著降低复杂度且不破坏语义时，才允许抽取。
12. 删除方法、类、变量上的冗余注释；清理 Javadoc 中重复、空泛或仅复述签名的信息，只保留语义说明、边界条件、异常约定等必要内容。
13. 复杂方法、复杂类、公共扩展点或使用方式不直观的能力，应补充必要 Javadoc；使用门槛较高时，给出简短使用示例。
14. 精准命令胜过陈词滥调：直接给出 `mvn -pl 当前模块 test`、`rg "类名|方法名" 当前模块/src/test` 这类具体步骤，避免“确保代码质量”“提升项目可维护性”等抽象套话。
15. 控制 `AGENTS.md` 长度与结构，建议保持在 200 行以内；规范继续增多时，在子目录创建领域指南，本文件只保留入口链接和必须遵守的红线。
16. 持续优化迭代：`AGENTS.md` 是动态文档；当 Agent 的实际输出与预期目标出现偏差时，把偏差改写成下一次可执行、可检查的规则。

## 1. 模块边界

`leaf-base-datasource` 是 Maple Leaf Framework 的数据源基础设施模块，只负责数据源、连接池、MyBatis-Plus 增强、数据权限、租户、基础 Repository/Service 支撑和相关 SQL 构建能力，不承载业务流程编排。

每次修改只能处理当前 Maven 模块范围内的代码、配置、测试和文档；不得随意修改其他 Maven 模块。确需跨模块修改时，必须说明原因、影响范围和兼容性。

## 2. 强制规则

1. 现有代码已经在生产环境使用，不得随意更改对外接口规范，包括公开类、接口、方法签名、泛型约束、配置键、异常语义和默认行为。
2. 在确保原有功能正确性的前提下进行完善；修改已存在逻辑时，不得改变现有逻辑的正确行为。
3. 本模块作为基础框架，性能和线程安全性非常重要；涉及共享状态、缓存、ThreadLocal、线程池、异步任务、SQL 构建或 SQL 拦截的改动，必须优先评估并发安全与性能影响。
4. 在线程池与虚拟线程都能满足需求的场景中，优先使用虚拟线程；若选择传统线程池，必须说明其必要性。
5. 所有 log 日志内容必须使用 ASCII 字符；新增或修改日志时禁止写入非 ASCII 文案。
6. 严格遵循测试驱动最佳实践：涉及改动的点，必须先查看现有测试是否完整；覆盖不足时必须补齐或更新测试用例。
7. 测试用例必须覆盖成功、异常、边界和回归场景；如果功能需要在 Spring Boot 应用运行后才能验证，测试必须模拟真实 Spring Boot 应用启动后的场景，例如使用 `@SpringBootTest`。
8. 不要随意将已存在代码抽取成单独的方法或类；只有抽取后的方法或类能被两个以上地方复用，且能明显提升可维护性时才允许抽取。
9. 清理方法、类、变量上的冗余注释，避免注释重复描述代码字面含义。
10. 清理 Javadoc 的冗余信息，只保留必要文档；复杂方法、复杂类和对外扩展点可以适当补充 Javadoc 与简短使用示例，便于使用者参考。
11. 新增配置项必须有默认值或清晰的失败语义，避免启动时出现隐式空值行为。
12. 涉及批量更新、批量删除或原始 SQL 的改动，必须保留条件非空保护。
13. 数据库能力必须符合 MyBatis-Plus 对多数据库的原生支持方式；新增 SQL 改写、分页、租户、数据权限或方言逻辑时，不得破坏不同数据库类型的兼容约定。

## 3. 既有行为红线

1. `GXDynamicContextHolder` 必须维持栈语义，`push` 与 `poll` 必须成对使用，禁止改成单值覆盖。
2. `GXDataSourceAspect` 必须在 `finally` 中恢复数据源上下文，禁止造成 ThreadLocal 泄漏。
3. `GXDataFilterAspect` 必须恢复旧的 `GXDataFilterInnerDto`，支持嵌套调用场景。
4. Druid filters 必须至少包含 `stat,wall,slf4j`，不得删除 `GXDynamicDataSourceFactory` 中的兜底逻辑。
5. `GXMyBatisPlusConfig` 中 `BlockAttackInnerInterceptor` 和分页插件的默认启用行为不得移除。
6. `maple.framework.enable.tenant=true` 时，缺失 `GXTenantIdService` 必须抛出异常。
7. `maple.framework.enable.data-permission=true` 时，缺失 `DataPermissionHandler` 必须抛出异常。
8. 自动填充字段 `createdBy`、`updatedBy`、`createdAt`、`updatedAt`、`tenantId` 的兼容行为不得破坏。
9. 异步监听任务必须透传数据源与数据权限上下文，即继续使用 `GXDynamicContextHolder.wrap` 和 `GXDataFilterThreadLocalUtils.wrap`。
10. `GXJoinDto` 的 `masterTableName/masterTableNameAlias` 表示 JOIN 子句中实际拼接的目标表和别名；`joinTableName/joinTableNameAlias` 主要用于白名单、逻辑删除和条件侧元数据解析。审查 `GXBaseBuilder.handleSQLJoin` 时不得将该设计误判为表名拼接错误。
11. `SELECT`、`GROUP BY`、`ORDER BY` 和 `HAVING` 默认保持旧项目兼容行为：`maple.framework.mybatis.sql.auto-underline-field-enabled=true` 时，涉及数据库字段的引用和 SELECT 表达式别名统一渲染为下划线格式。新项目如需保留调用方传入的字段和 SELECT 别名拼写，并让 GROUP/ORDER/HAVING 按已选字段或别名原样渲染，可显式设置 `maple.framework.mybatis.sql.auto-underline-field-enabled=false`。

## 4. 代码演进

1. 优先沿用本模块既有抽象、命名风格和工具方法，不为局部需求引入新的架构层。
2. 通用能力优先下沉到 `GXMyBatisRepository` 或 `GXMyBatisBaseServiceImpl`，不要散落在业务模块中。
3. Spring 注入优先使用 `jakarta.annotation.Resource`；仅在集合注入、泛型注入或必须配合 Spring 语义时使用 `@Autowired`。
4. 异常类型优先使用框架统一异常，例如 `GXBusinessException`，避免裸抛 `RuntimeException`。
5. 对外可见方法的失败语义必须明确，不能吞掉关键失败。

## 5. 测试要求

1. 每次涉及行为变更的修改，都必须同步检查测试用例是否完整。
2. 优先补充小而明确的单元测试；涉及 Spring Boot 装配、切面、拦截器、配置加载、租户、数据权限、异步上下文传播等运行态能力时，必须补充能启动真实 Spring Boot 上下文的测试。
3. 数据源上下文、数据权限上下文、租户开关、分页、防全表更新、Druid 初始化失败清理、自动填充和 SQL builder 行为都属于重点回归范围。
4. 任何可能导致全局 SQL 行为改变的改动，必须附最小复现用例或测试说明。

## 6. 变更前检查清单

1. 是否保持生产环境已使用的对外接口规范、默认行为和异常语义兼容？
2. 是否只修改了当前 Maven 模块范围内的代码、配置、测试和文档？
3. 是否保护了现有正确逻辑，没有为了完善功能而破坏原有行为？
4. 是否评估过性能、线程安全性和线程模型选择？
5. 是否影响 `@GXDataSource`、`@GXDataFilter`、租户、数据权限、分页、防全表更新或异步上下文透传？
6. 是否符合 MyBatis-Plus 多数据库兼容约定？
7. 是否已经查看并补充必要测试，且测试覆盖成功、异常、边界和回归场景？
8. 是否移除了冗余注释和冗余 Javadoc，并为复杂用法保留必要文档或示例？
9. 是否确认新增或修改的日志文案均为 ASCII？
